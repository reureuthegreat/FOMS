package com.Category;

/**
 * Represents an interface for reading categories.
 */
public interface ICategoryReader {
	/**
	 * Reads a category input and returns the corresponding Category enum.
	 *
	 * @return A Category enum representing the read category.
	 */
	Category readinCategory();
}
